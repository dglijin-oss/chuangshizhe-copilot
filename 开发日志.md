# 开发日志

> 架构评审与重构规划

---

## 一、当前架构优点

1. **数据模型设计扎实** — Prisma schema 结构完整，双系统实体定义清晰，关系和索引合理
2. **认证系统一致** — `auth.ts` 提供统一认证入口，Session 安全属性设置正确
3. **UI 组件风格统一** — Glass 系列组件组织良好，视觉一致
4. **积分/佣金体系完整** — 扣费、分佣、流水逻辑闭环完整

---

## 二、问题清单

### 🔴 严重

| # | 问题 | 影响 |
|---|------|------|
| 1 | **巨型组件** — `ip/page.tsx` 1638 行、`admin/page.tsx` 821 行、`collection/page.tsx` 440 行 | 维护困难，加功能越来越慢 |
| 2 | **采集系统权限缺失** — `/api/collection/*` 路由只验证登录，不验证操作权限，任何登录用户可改删他人数据 | 安全风险 |
| 3 | **Cron 路由无鉴权** — 任何人都能触发采集任务 | 安全风险 |

### 🟠 高优先级

| # | 问题 | 影响 |
|---|------|------|
| 4 | **全局 `any` 类型** — 约 15 处 `useState<any[]>()`，TypeScript 形同虚设 | 运行时 bug 难排查 |
| 5 | **API 无统一错误处理和输入校验** — 49 个路由各自处理，分页/边界无拦截 | 容易被异常数据搞崩 |
| 6 | **采集任务同步执行** — HTTP 请求上下文中直接调外部 API，超时即失败 | 不稳定 |

### 🟡 中等优先级

| # | 问题 | 影响 |
|---|------|------|
| 7 | **业务常量硬编码散布多处**（行业选项、佣金比例等） | 改一处漏一处 |
| 8 | **IPProfile 的 JSON 字段以字符串存**，DB 层不可查询 | 后期扩展受限 |
| 9 | **中间件 `PUBLIC_PATHS` 数组定义了但没被复用** | 容易遗漏 |

### 🟢 低优先级

| # | 问题 | 影响 |
|---|------|------|
| 10 | **零测试覆盖** | 回归风险 |
| 11 | **`wechat.ts` 空壳未实现** | 功能缺失 |
| 12 | **`store.ts` 是死代码** | 无实际影响 |

---

## 三、重构规划（分 Phase 渐进）

### Phase 1：拆分巨型组件（最优先）
- 把 `ip/page.tsx` 拆成子组件：`workspace/`、`corpus/`、`settings/` 等
- 替换所有 `any` 为 TypeScript interface

### Phase 2：补全采集系统安全（3-5 天）
- 采集 API 加权限校验
- Cron 路由加 secret 验证

### Phase 3：基础设施（1 周）
- 统一 API 错误处理 + 输入校验
- 采集任务改为异步

### 后续（不急）
- 数据库 schema 微调（JSON → Json 类型）
- 去重 `IntelligenceSource` 和 `MonitoredAccount` 概念

---

## 四、总结

当前系统能跑，不需要推倒重来。主要技术债集中在：
1. **前端巨型组件**
2. **采集系统的权限缺失**

按 Phase 逐步拆即可。
