# 2026-05-20 开发日志

## 1. 抖音无字幕视频回退处理

- 用户测试抖音视频 `7352364040410221843` 发现没有自动字幕
- 修改 `fetchDouyinSubtitle` 和 `extractSubtitle`：无 `caption_url` 时回退到视频描述
- 不再抛错，显示 `[无自动字幕，已提取描述]`

## 2. 采集系统改用 Hermes Agent

- 用户提出接入 Hermes 智能体替代 TikHub
- 调研 Hermes API（OpenAI 兼容 HTTP 接口，SSE 流式响应）
- 方案 C：Hermes 完全接管采集页面，改为聊天界面
- 新建 `src/app/api/hermes/chat/route.ts`（SSE 代理）
- 重写 `src/app/(app)/scrape/page.tsx`（聊天界面 + 流式响应）
- 删除 `src/lib/tikhub.ts`、`scrape-config.ts`、`url-extractors.ts`、`api/scrape/` 路由
- 代码改动：406 行新增，废弃文件全部清理

## 3. 切换为 OpenClaw

- 用户决定不连 Hermes，改连同一台服务器上的 OpenClaw
- 调研 OpenClaw 架构：Docker 容器 `1panel/openclaw:2026.5.18`，端口 18789
- 启用 `chatCompletions` 端点（默认未开启），重启容器
- 验证 `/v1/chat/completions` SSE 流式响应正常
- copilot 容器用 `--network host` 模式，通过 `http://127.0.0.1:18789` 内网直连 OpenClaw
- 无需开放 18789 端口到公网
- 更新 `.env`：`OPENCLAW_API_URL=http://127.0.0.1:18789` + Token
- 重新构建 Docker 镜像并部署
- 遇到 OpenClaw 网卡在 draining 状态，强制重启解决
- 用户反馈 OpenClaw 返回 "Error: internal error"，定位为 draining 导致

## 4. OpenClaw 权限问题排查

- 用户飞书安装插件报"权限不够"
- 排查：飞书 ID `ou_934b27f6a52dd008a617a99a827287e6` 已在 `ownerAllowFrom`，权限正常
- 实际是重复安装提示 "Skill already exists"，非权限问题
- 已安装技能：`account-analyzer`、`zc-douyin-xiazai-txt`
- 用户担心 OpenClaw operator 权限过大，讨论加固方案
- 最终决定先观察，暂不做限制

## 5. 计划切换回 Hermes Agent

- 用户反馈 OpenClaw 太笨，在同一台服务器安装 Hermes Agent
- 等待 Hermes 安装完成后切换代码
